import React from "react";
import Icon from "../components/Icon";

const Express = ({ ...props }) => (
  <Icon className="devicon-express-original-wordmark colored" {...props} />
);

export default Express;
