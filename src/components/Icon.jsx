import styled from "styled-components";
import {
  border,
  flexbox,
  space,
  layout,
  color,
  position,
  typography,
  shadow,
} from "styled-system";

const Icon = styled.i`
  ${border}
  ${flexbox}
  ${space}
  ${layout}
  ${color}
  ${position}
  ${typography}
  ${shadow}  
`;

export default Icon;
