import styled from 'styled-components';
import { border, space, layout, color, position, typography, shadow } from 'styled-system';

const Text = styled.p`
  ${border}
  ${space}
  ${layout}
  ${color}
  ${position}
  ${typography}
  ${shadow}
 
`;

export default Text;
